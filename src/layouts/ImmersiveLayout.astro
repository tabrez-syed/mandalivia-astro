---
import BaseHead, { type Props as HeadProps } from '../components/BaseHead.astro';

export type Props = HeadProps;

const { ...head } = Astro.props;
---

<!doctype html>
<html lang="en">
    <head>
        <BaseHead {...head} />
    </head>
    <body>
        <!-- Debug indicator -->
        <div style="position: fixed; top: 0; right: 0; background: red; color: white; padding: 4px;">Immersive Layout</div>
        <main>
            <slot />
        </main>
    </body>
</html>
